import { ScaleDirectiveDirective } from './scale-directive.directive';

describe('ScaleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
